import { PropsWithChildren } from 'react'

// import { CommonErrorInterceptor } from '@/widgets/configs/interceptors/CommonError'

export const Mandatory = ({ children }: PropsWithChildren) => {
  return <>{children}</>
}
